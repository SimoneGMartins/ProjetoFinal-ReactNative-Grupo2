import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  button: {
    width: '80%',
    padding: 15,
    borderRadius: 12,
    backgroundColor: '#C1272D',
    marginBottom: 16,
  },

  text: {
    color: '#FFFFFF',
    fontSize: 18,
    fontWeight: 'bold',
  },
});
